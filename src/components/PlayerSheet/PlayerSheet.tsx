import React, { FC } from 'react';

interface PlayerSheetProps {}

const PlayerSheet: FC<PlayerSheetProps> = () => (
  <div data-testid="PlayerSheet">
    PlayerSheet Component
  </div>
);

export default PlayerSheet;
